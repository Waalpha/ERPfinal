rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for multi-tenant ERP operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}


